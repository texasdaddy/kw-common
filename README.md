# kw-common

Shared, **stdlib-only** building blocks for a small fleet of Python services.

Repository bootstrap. The library, its tests and its CI arrive in the first pull request; this
commit exists so that pull request has a `main` to target.
