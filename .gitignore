# Claude instructions stub (points to the project-stored instruction file; never committed)
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
# `.venv*/`, not `.venv/`: this project is tested on two interpreters, so a working tree
# routinely holds `.venv` beside `.venv310`. The narrower pattern left the second one UNTRACKED
# and therefore visible to `git add -A`.
.venv*/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db
